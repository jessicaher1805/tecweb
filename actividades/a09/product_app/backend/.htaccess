# Habilitar el motor de reescritura
RewriteEngine On

# Establecer la base para las reescrituras - AJUSTA ESTA RUTA
RewriteBase /tecweb/actividades/a09/product_app/backend/

# Si el archivo o directorio solicitado no existe físicamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirigir todas las peticiones a index.php
RewriteRule ^ index.php [QSA,L]